# PickPico 0.16.79

## 佈景選色修正

- 從遠端 main 的 `d017222`（0.16.78）接續修改。
- 移除黑白捷徑與固定色票。Color A／Color B 改用可點選、拖曳的連續調色盤，搭配彩虹色相條與六位色碼輸入。
- 原本固定色票逐一設定滑桿時，共用 HSV 陣列會被滑桿事件覆寫，導致後續飽和度、亮度取到錯誤數值。新版統一同步調色盤、色相和色碼，避免互相覆寫。
- 按 Apply 直接保存所選顏色，並同步更新背景、玻璃、文字與 PICO 預覽；取消保留原色，無效色碼會提示修正。
- 單色模式隱藏不參與顯示的 Color B；開啟漸層後恢復，先前選色仍保留。
- 原版／火焰版 PICO 切換及既有玻璃效果設定保留。

## 驗證

- Android 測試 195 項通過，debug APK 0.16.79（116）建置成功。
- 新增 Android 介面測試：兩個顏色按鈕開啟調色盤、Apply 更新當前與保存的佈景、重開頁面保持顏色、取消保留原色、無效輸入不關閉視窗。
- 新增調色盤測試：觸控取色、色碼與色相同步、黑白端點，以及無效輸入檢查。
- 建置時手機 PickPico 連線曾連續回傳 Internal error，ADB 沒有可用裝置；發布時連線已恢復。調色盤尚未完成真機操作驗證。

## 正式更新頻道

- 使用者要求發布後，於 2026-09-18 15:08:50（Asia/Taipei）完成 stable 0.16.79（116）發布；程式提交為 `8cc2849`。
- Relay 測試 5 項通過，沿用原有 APK 簽章；本次只更新安裝檔及更新清單，沒有重新部署 Relay 程式。
- 公開更新清單 `/v1/update/latest` 回報 0.16.79（116）。公開下載 APK、更新清單與本地 APK 的 SHA-256 相符：`676c08a1aa062d187b507db030eae5df89b5e2676fded154b5abbbc1221c5729`。
- 已確認同一台 Samsung SM-S9110 的連線與 Node 身分，手機原版本為 0.16.78，並已透過 `app.update_latest` 啟動正式版下載。
- 15:10 再次查詢同一台手機，已回報 0.16.79、Relay 連線正常，確認安裝完成；不將此版本確認當成調色盤真機操作測試。
