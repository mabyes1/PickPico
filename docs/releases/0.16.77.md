# PickPico 0.16.77

## 畫面操作備援

- 有可用的無障礙元件時，繼續使用元件定位。
- `ui.action` 可使用最新截圖的像素座標點擊或長按。
- `ui.scroll` 可使用最新截圖的起點與終點執行滑動。
- Android 13 以上可在畫面欄位已聚焦後，透過輸入連線完成無元件文字輸入。
- 最新截圖的操作識別值只能使用一次，逾時、重用、前景改變或座標超出畫面都會在操作前拒絕。
- 操作指南改為「有元件用元件；沒有元件看截圖操作」，每次操作後必須重新觀察結果。

## 驗證

- Android 單元測試 186 項通過，debug APK 建置成功。
- Relay 測試 5 項通過。
- Samsung SM-S9110、Android 16 真機驗證通過：Samsung Browser 只提供 `SurfaceView` 時，仍可用截圖座標完成點擊、輸入及儲存；只讀狀態確認 `recipientSaves=1`。
- 真機滑動與長按通過；舊截圖回覆 `STALE_SCREEN`，畫面外座標回覆 `POINT_OUT_OF_SCREEN`，兩者都沒有執行操作。
- 完整真機證據保存在本機 `artifacts/visual-ui-dev/VISUAL-20260912-1800-report.md`，測試 Node 程序已停止。

## 已知檢查狀態

`lintDebug` 仍被既有的 `AgentAttention.java:116` `WrongConstant` 擋住；該行在本次修改前的提交已存在。本次新增的視覺操作檔案沒有出現在 lint 錯誤中。

發布完成後補記公開更新入口、APK 雜湊與程式提交。
